# dev symlinks into the sibling data repo (../acumatica-baseline)
# root-anchored: src/acumatica_cli/templates/ ships files with these names
/baseline
/bootstrap
/setup
/acu.yaml
/.env

# dumped API schemas — regenerate with acu schema
schemas/

# python
.pytest_cache
.ruff_cache/
.venv/
__pycache__/
