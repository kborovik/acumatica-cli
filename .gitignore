# dev symlinks into the sibling data repo (../acumatica-salesdemo)
# root-anchored: src/acumatica_cli/templates/ ships files with these names
/baseline
/bootstrap
/setup

# instance config at repo root — decrypted secrets and their ciphertext, never committed
/.env

# dumped API schemas — regenerate with acu schema
schemas/

# python
.pytest_cache
.ruff_cache/
.venv/
__pycache__/
