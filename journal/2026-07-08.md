# 2026-07-08

## Morning — recycle unblocks the tenant; the "foot-gun" was a stale-map artifact; first-login password is the real bootstrap wall

Ran the app-pool recycle that was pending operator approval yesterday
(`Restart-WebAppPool -Name AcumaticaERP` over SSH). It cleanly resolved the
visibility problem **and corrected two conclusions from last night** — both
worth recording, because "the thing I was sure of was an artifact" is the
honest shape of this kind of work.

**The recycle works — tenant 3 is now real.** After the recycle the sign-in
page lists `Scratch`, and REST tenant routing turns *strict*. So the fix is
verified: `acu tenant create` must recycle the `AcumaticaERP` app pool after
`ac.exe` returns, or the new tenant stays invisible to the running app until
something else recycles it.

**Correction to Surprise #2 — the silent foot-gun was the stale map, not the
API.** Last night I "proved" that REST login accepts *any* tenant name and
silently lands on the default. Re-tested post-recycle, that's no longer true,
and the difference is the tenant map being current:

- `tenant='DefinitelyBogus'` (unknown, non-empty) → **500
  `PXUndefinedCompanyException`** ("A proper company ID cannot be determined").
  No silent fallback anymore — unknown names are rejected.
- `tenant='Scratch'` (valid name) → routes to tenant 3 and enforces *tenant
  3's* credentials (returned invalid-credentials, **not** a silent success on
  tenant 2's password). Proof the routing is per-tenant now.
- `tenant` omitted or `''` → still 204 on the default tenant. This is the only
  residual silent behavior, and it's expected Acumatica semantics (no tenant =
  default tenant).

So the real, narrower rule for the CLI: **always send an explicit, valid
tenant name — never omit it, never trust an empty value.** The dramatic
"writes to a nonsense tenant" scenario was specifically a symptom of skipping
the post-create recycle (tenant in the DB, absent from the loaded map). With a
current map, a wrong name errors loudly. A post-login tenant guard is still
worth having as defense-in-depth, but it's a seatbelt, not a firewall.

**The real bootstrap wall — first-login password.** With tenant 3 finally
reachable, the honest virgin-tenant login story emerges (last night's "login
is NOT a blocker" was itself an artifact — those probes hit tenant 2, which is
long past first login):

- A freshly created clean tenant seeds `admin` with password **`setup`** and
  `PasswordChangeOnNextLogin = 1` (confirmed in `dbo.Users`).
- The contract REST login **refuses to perform the change**: passing
  `newPassword` alongside `setup` still returns *"You need to change your
  password. Sign in by using your browser to set a new password."* The
  `/entity/auth` endpoint can authenticate but not clear the first-login flag.
- Repeated wrong-password attempts **lock the account** (`LockedOutDate` gets
  stamped) — my probe loop tripped it on tenant 3.

Consequence: **the first-login password change is a bootstrap step the
contract REST API cannot do.** `acu provision` has to handle it out of band —
either drive the screen-based login (`Login.aspx`, which is what the error
tells you to use), or, as part of `acu tenant create`, clear
`PasswordChangeOnNextLogin` and set a known admin password in the DB so the
tenant comes up automation-ready. Design call deferred to the provisioning
work; documented in `docs/rest-api.md`.

**Next.** (1) Decide the first-login password strategy (screen login vs. SQL
reset at create time) and bake it into `acu tenant create`; unlock tenant 3.
(2) With a genuinely loginable virgin tenant, re-run the entity/CustomizationApi
probe against tenant 3 to confirm last night's map (all gathered against
tenant 2) holds on a real fresh tenant. (3) Author the bootstrap package. The
route decision from last night stands — CustomizationApi is still the only
door to features + company/branch.

## Afternoon — screen-login password change prototyped; virgin-tenant map confirmed

The first-login wall from this morning fell to ~60 lines of httpx + stdlib
`HTMLParser` — no browser, no Selenium, no SQL poking. The `Login.aspx`
WebForms flow is fully scriptable:

1. `GET /Frames/Login.aspx` → harvest `__VIEWSTATE` & friends + session
   cookie.
2. `POST` back with `cmbCompany=Scratch`, `txtUser=admin`, `txtPass=setup`,
   `btnLogin=Sign In` → the server re-renders the page **with
   `txtNewPassword` / `txtConfirmPassword` now present** — that's the
   password-change view.
3. `POST` again with old + new + confirm filled → redirect to `/Main`,
   signed in. REST login with the new password: **204**.
   `PasswordChangeOnNextLogin` flipped to 0 in `dbo.Users`.

**Errors & traps (all hit):**

- First attempt posted the wrong button — the page has *four* submit inputs
  and `mfLoginButton` (multi-factor) matches "login" first; the right one is
  `ctl00$phUser$btnLogin`.
- My field regex filled `txtNewPassword` but missed `txtConfirmPassword` →
  silent re-render, no change. Both must be posted.
- The page markup *always* contains alarming hidden divs ("Last update was
  unsuccessful", "customization failed to apply automatically after the
  upgrade") — static templates, not responses to your POST. Cost me a
  double-take.
- The login JS sets an `isReal` click flag, but it's client-side theater — a
  plain postback is accepted.

**Decision:** screen-flow automation wins over the SQL-reset alternative — it
uses a supported surface, exercises the same path a human would, and leaves
`dbo.Users` untouched. `acu tenant create` should chain: `ac.exe` → app-pool
recycle → screen-flow first-login change → tenant is automation-ready.

**Virgin-tenant map confirmed.** With tenant 3 genuinely loginable, re-ran
the entity probe against it: identical three-class map (UOMs/FinancialYear
work; `PXSetupNotEnteredException` on the Company-Branches chain; 403 on
feature-gated screens), `PUT CompaniesStructure` still dead, CustomizationApi
answers 200 "no published projects". Yesterday's conclusions — gathered
accidentally against tenant 2 — stand on a real fresh tenant. Bogus tenant
names still rejected loudly (strict routing held).

**Next.** (1) Wire recycle + first-login into `acu tenant create`. (2) Author
the bootstrap package and publish it to Scratch via the CustomizationApi.
(3) `acu provision` to chain the whole pipeline.

## Evening — `acu tenant create` chains the whole thing; two more ac.exe landmines defused

Wired `ac.exe → recycle → login-ready` into `acu tenant create` and made it
work end to end against a real fresh tenant (id 4, `Scratch2`): create,
app-pool recycle, REST login verified — one command, no manual steps.
`acu tenant delete` round-trips it too. But getting there tripped two more
CompanyConfig landmines that the ~40-second happy path from yesterday hid.

**Landmine 1 — `-iname` isn't enough; you also need `-h` (instance path).**
The first CLI create blew up mid-run:
`System.ArgumentNullException: … path1` in
`AcumaticaConfig.Installation.WebCompanyConfig()`, and the whole thing rolled
back. Yesterday's manual creates happened to dodge it (they ran far enough to
insert data before the web.config stage, or the earlier invocation differed);
the moment it ran clean from the CLI it surfaced. Fix: pass
`-h:"C:\Acumatica\AcumaticaERP"` (found via the IIS site's physical path in
the registry). Added `instance_path` to the instance YAML alongside
`instance_name`.

**Landmine 2 — delete is a minefield of its own.** Two nested traps:

- The sub-key is **`Deleted`**, not the officially documented `Delete`.
  `Delete=Yes` is silently ignored — and the run then treats the tenant as a
  *data-insert target and overwrites it*. (This is what mangled tenant 4 into
  a half-System husk mid-debug.)
- Even with `Deleted=Yes`, a bare `CompanyID=N;Deleted=Yes;` aborts at the
  preflight (`dataCpnTest` → `CompanyMaint.CheckAndUpdate`) with
  **"The system company cannot be removed."** The preflight reads the missing
  `CompanyType` as the system tenant. Passing the full spec
  (`ParentID=1;CompanyType=Custom;Deleted=Yes`) clears it. `delete()` now
  looks the tenant up first and echoes back its real type.

**Correction — `LoginName` *did* take, I looked in the wrong column.**
Yesterday: "LoginName=Scratch didn't become CompanyCD, got auto-named
Company2." Today: `LoginName` lands in `dbo.Company.CompanyKey`, which is
exactly the sign-in name and the REST `tenant` value. `CompanyCD` is just an
auto-generated internal code. So the tenant was named correctly all along;
`acu tenant list` now reads CompanyKey as the login name.

**Simplification — skip the screen dance entirely on create.** The
`-aun`/`-aup`/`-auc` admin-preset flags set the new tenant's admin password
up front with no must-change flag, so REST login works straight after the
recycle. The `Login.aspx` form flow from this afternoon still ships as the
fallback (for tenants born without the preset), but the create path no longer
needs it — `initialize_admin_password()` just confirms the REST login and
returns "already initialized". Nice outcome: the fussy WebForms scraping is
now belt-and-suspenders, not the critical path.

Net: `acu tenant create --id N --login NAME` is a real one-liner now.

## Late — tenant login names can be domain-style (`lab5.ca-dev1`)

Question that matters for the naming scheme: can a tenant's sign-in name be a
domain-shaped string like `lab5.ca` / `lab5.ca-dev1`? Tested the hardest case
(dot **and** hyphen) end to end on the box: `acu tenant create --id 4 --login
lab5.ca-dev1` → ac.exe accepted `LoginName=lab5.ca-dev1` (exit 0), it landed
**verbatim** in `dbo.Company.CompanyKey`, `acu tenant list` shows it as the
sign-in name, and a REST login with `{"tenant":"lab5.ca-dev1"}` returned **204**
under strict post-recycle routing (resolved the dotted name, not a silent
fallback). Clean delete round-trip after.

Bounds worth stating: safe alphabet is alphanumerics + `.` + `-`. The two
characters to avoid are `;` and `=` — the `-company:"…;LoginName=X;"` string
uses them as delimiters, so a name containing either would corrupt the ac.exe
invocation (a validation/escaping concern for `create` if names ever come from
untrusted input). Spaces / `@` / `/` untested. `CompanyKey` carries the pretty
name; `CompanyCD` stays the auto code (`Company3`). So per-environment tenant
names — `lab5.ca-dev1`, `lab5.ca-prod`, … — are viable.

## CLI output standards — rich, two audiences, one code path

Housekeeping before the CLI grows more commands: defined output standards
(`docs/cli.md`) and adopted [rich](https://rich.readthedocs.io), replacing the
bare `print()`s scattered across `cli.py`/`seed.py`.

The framing that made the design easy: the CLI has **two audiences** — humans
at a terminal, and LLM agents / scripts reading piped output. rich's `Console`
serves both from one code path: TTY → colors, rounded tables, spinners;
piped → plain deterministic text (no ANSI, no box-drawing). Verified both
renderings live against acu-dev1.

Mechanics: new `output.py` is the only module that creates a `Console`
(stdout = data/results, stderr = status/progress/errors; `✓`/`!`/`✗`
prefixes survive piping so agents can grep them). Long ops (`tenant create`
steps) wrap in `output.step()` — spinner on TTY, plain stderr line piped.
A top-level handler in `cli.main()` turns `RuntimeError`/`httpx.HTTPError`
into a one-line `✗ message` + exit 1 (`ACU_DEBUG=1` re-raises the traceback)
— previously every SSH/REST failure dumped a full Python traceback. The
`diff` exit-1-on-drift contract is unchanged (re-verified: `✓ no drift`,
exit 0). Enforcement: ruff `T20` now fails the build on any bare `print()`.

Two details worth recording: markup/emoji/highlighting are disabled on both
consoles — apply lines like `PUT Currency [CAD]` contain brackets rich would
otherwise eat as markup; and `tenant list` switched from tab-separated rows
to a proper table (nothing parsed the tabs — checked Makefile/scripts first).

Follow-up polish: login now goes through `_checked()` like every other call,
so a failed login shows Acumatica's own message (`✗ POST …/auth/login -> 500:
A proper company ID cannot be determined for the request.`) instead of
httpx's generic status line with an MDN link.

## Test suite established — 45 offline tests, no live instance required

Until now every verification was live ("run it against acu-dev1"), which
means no check at all without the tailnet path. Established `tests/`
(pytest 9): `make test`, and `make check` = lint + tests. Everything runs
offline in <0.1s.

The two seams that made it cheap:

- **REST:** `AcumaticaClient` grew an optional `transport` param (the only
  product change) so tests inject `httpx.MockTransport`. seed's apply/diff
  tests run through the *real* client — wrap, `$filter`, `_checked` — against
  canned responses; no hand-rolled fakes to drift out of sync.
- **SSH:** `subprocess.run` monkeypatched. The tenant tests pin the exact
  ac.exe command lines — which turns today's landmines into regression tests:
  `-iname` **and** `-h` both present, `Deleted=Yes` with the full
  `ParentID`+`CompanyType` spec, the `-aun/-aup/-auc` admin preset, and the
  trailing `exit $LASTEXITCODE`.

Also pinned: diff's exit-1-on-drift contract (CliRunner), logout's
`Content-Length: 0` and logout-runs-even-on-failure, the `✓`/`✗` stream
routing from docs/cli.md, and load_instance's env merging.

Two of the first 45 tests failed on first run — both test bugs, both
informative: rich wraps a table *title* to the table's content width when
piped (so don't assert on an unbroken title string), and a diff test's canned
response matched every `$filter`, reminding that `seed.diff` trusts the
filter rather than re-checking key equality client-side. basedpyright strict
now covers `tests/` too.

## pydantic as the model standard; instances/ folded into acu.toml

Two related structural moves, no live-instance behavior change (50 offline
tests green; `make check` clean):

**pydantic replaces dataclasses everywhere.** New `models.py` defines the
standard: every structured value crossing a boundary inherits `Model`
(`BaseModel` with `frozen=True, extra="forbid"`). `Instance`, `Tenant`, and
`BaselineFile` converted. What this buys over the frozen dataclasses:

- *Validation moves into the model.* `BaselineFile`'s hand-rolled checks
  (required fields, key-listification, every-record-carries-its-keys) became
  a `field_validator` + `model_validator`; `Instance` normalizes
  `base_url`/`endpoint` slashes in validators instead of in `load_instance`.
- *Typos are now errors.* `extra="forbid"` rejects unknown fields in both
  `acu.toml` tables and baseline YAML — previously a misspelled `db_nmae`
  or stray baseline key was silently ignored.
- `dataclasses.replace` → `model_copy(update=...)` (CLI `-t` override).
- `validation_summary()` renders pydantic's `ValidationError` into the
  one-line `✗`-friendly form docs/cli.md wants.

One convention shift: pydantic models take keyword args only, so `Tenant(1,
"System", ...)` in tests became explicit kwargs — noisier but self-labeling.

**`instances/*.yaml` → `acu.toml`.** One TOML config file at the repo root
instead of a YAML file per instance: `[instances.<name>]` tables plus a
top-level `default_instance`. The CLI's `-i` default moved out of the code
(`cli.py` no longer hardcodes `acu-dev1`) and into config where it belongs.
Reading is stdlib `tomllib` — PyYAML stays only for baseline files. TOML
literal strings (`'C:\Program Files\...'`) also read better for the Windows
paths than YAML's escaping.

## Repo split: acu is the tool; acumatica-baseline is the first data repo

Objectives redefined. This repo is now **only the `acu` CLI**; the data it
applies moved to a new self-contained sibling repo, `acumatica-baseline`:
`baseline/*.yaml` (what), `acu.toml` (where), `.env.gpg` (secrets). Clone the
data repo, decrypt, run one command — the "byte-identical tenant" definition
of done now belongs to data repos, of which there can be many.

The discussion that led here started as "should `acu config get/set` manage a
config file in `~/.acu/`?" — rejected for this repo's original shape (in-repo
`acu.toml` *is* the config-as-code), but the underlying itch was real: the
tool and the data wanted to be separate. Splitting the repos scratches it
without giving up version-controlled config: the data repo carries everything,
and `~/.acu/` stays unnecessary.

Mechanics — the seam was already clean (`apply`/`diff` take explicit file
paths; `baseline/*.yaml` was hardcoded only in the Makefile), so the only
code change is discovery in `config.py`: `repo_root()` (walk up from cwd to
`pyproject.toml`) became `data_root()` (walk up to the first dir containing
`acu.toml`, hard error with a pointer to data repos if none — no silent cwd
fallback). `.env` loads from the same directory as the found `acu.toml`.
No `ACU_ROOT` env override for now — YAGNI with one data repo.

How the data repo invokes the tool: `uv tool install --editable
~/github/acumatica-devops` (its `make install`), putting `acu` on PATH with
tool-repo source edits live. Rejected: `uv run --project ../acumatica-devops`
(hardcodes a relative sibling path into every Makefile target) and `uvx
--from` (re-resolves per run, not editable).

What moved where: `baseline/`, `acu.toml`, `.env.gpg`/`.env`, and the
`tenants/apply/diff/encrypt/decrypt` Makefile targets went to
`acumatica-baseline` (plain `mv` — history stays here). This repo keeps the
Python project, `docs/`, `journal/`, `tests/` (already fixture-based, only
the `pyproject.toml` sentinel in `test_config.py` needed changing; two new
tests cover parent-dir discovery and the missing-`acu.toml` error), and
`scripts/dump-swagger.sh` + `schemas/` (env-var driven dev aid).

Post-split dev ergonomics: the tool repo carries gitignored symlinks —
`baseline`, `acu.toml`, and `.env` → `../acumatica-baseline/` — so `acu`
still runs from here during development. `.env` had to be linked alongside
the two requested paths because `data_root()` loads it from the directory
where it finds `acu.toml`; without it, live commands from the tool repo fail
with "ACU_PASSWORD not set". Ignored rather than committed: a clone without
the sibling repo would just get dangling links.

Small CLI fix: `acu diff baseline/` crashed with a raw `IsADirectoryError`
traceback — click's `Path(exists=True)` accepts directories, and
`load_baseline` then `open()`s one. `apply`/`diff` now expand directory
arguments into their sorted `*.yaml` files (`expand_files` in cli.py; empty
dir is a clean error), so `acu diff baseline/` matches the Makefile's
`baseline/*.yaml` glob behavior without depending on the shell.

## `scripts/dump-swagger.sh` → `acu schema`

The swagger-dump shell script was the last holdout from before the CLI had
real infrastructure, and it had quietly become a liability: a second curl
implementation of login/logout (re-encoding the license-session and
`Content-Length: 0` quirks `AcumaticaClient` already owns), a second config
channel (`ACU_URL`/`ACU_USER`/`ACU_ENDPOINT` env vars, manually kept in sync
with `acu.toml`), and zero test coverage — none of the offline suite's seams
reach a bash script. Converted it to `acu schema [-o DIR]`: one new
`AcumaticaClient.swagger()` method (raw bytes off
`/entity/<endpoint>/swagger.json`), default output
`<data_root()>/schemas/swagger-<endpoint>.json` so the dump follows the data
repo the command runs from. Script deleted; docs/README/.gitignore
references updated. Verified live: same path, byte count identical to
yesterday's script-produced dump (3,068,987 bytes).

One finding the new test surfaced: `output.data()` hard-wrapped long lines
at rich's default 80-column width when piped — a schema path (or a long
drift line) came out split across two lines, breaking the "greppable
one-line results" contract in docs/cli.md. `data()` now prints with
`soft_wrap=True`; the rule is written into docs/cli.md.

## Repo renamed: acumatica-devops → acumatica-cli

The split left the repo with the vaguest name in the family:
`acumatica-infra`, `acumatica-baseline`, and `acumatica-blog` all say what
they contain; `acumatica-devops` said a vibe — and arguably described the
infra repo better than this one. The tell: the README's first line had to
immediately correct the name ("# acumatica-devops / The **`acu` CLI**").
Renamed to **`acumatica-cli`** — keeps the `acumatica-` prefix so the four
repos sort together.

The rename went all the way down rather than stopping at the directory:

- **Package `acu-devops` → `acumatica-cli`, module `acu_devops` →
  `acumatica_cli`.** These move together: the `uv_build` backend derives the
  module directory from the project name, so renaming the project alone would
  have needed a `module-name` override in `pyproject.toml` — and would have
  left the dead name in every import anyway. Imports, isort/basedpyright
  config, the `acu` entry point, and `@click.version_option(package_name=…)`
  (which resolves the version from package metadata at runtime — easy to
  miss) all updated. README rewritten under the new name.
- **No GitHub remote existed** — the `github.com/kborovik/acumatica-devops`
  link in the baseline README was dead all along. It now points at
  `acumatica-cli`, still aspirational until the repo is pushed.
- **Cross-repo touchpoints:** `acumatica-baseline`'s `ACU_TOOL` path and
  README updated; the `uv tool install --editable` entry pointed at the old
  path (editable installs pin an absolute path), so `acu` was reinstalled via
  the baseline repo's `make install`. The dev symlinks needed nothing —
  they're relative and point *out* of this repo.

Verified: `make check` green (57 offline tests) and `acu --version` resolves
the new package name post-reinstall.

## Evening — spec-driven: CLAUDE.md + docs/ distilled into SPEC.md

Adopted the sdd workflow: distilled CLAUDE.md and the three verified docs
(`ac-exe.md`, `rest-api.md`, `cli.md`) into a root `SPEC.md` — goal,
constraints, interfaces, 14 invariants (two-plane split, source-kind
separation, recycle-after-create, context-manager client, output contract,
…), and 4 pending tasks (`acu provision`, bootstrap package, CS100000
write-path verification, post-login tenant guard). CLAUDE.md reduced to a
pointer stub: spec is authoritative, docs stay as the verified deep
references. No friction — pure documentation restructuring.

## Night — repo published

Initial commit (29 files, 3580 lines) pushed to a new public GitHub repo:
https://github.com/kborovik/acumatica-cli. The "aspirational" remote from
the morning rename is now real. Pre-push check confirmed the dev symlinks
(`baseline`, `acu.toml`, `.env`) stayed gitignored and no credentials landed
in the staged tree.

## Late night — /sdd:build --all: provision + bootstrap machinery land; the custom-endpoint archaeology; CS100000 says no

Ran the full pending task backlog (`/sdd:build --all`). Three tasks landed
as straight offline-verified code; the fourth (T3, the CS100000 write-path
verification) turned into a deep live-instance archaeology session that
rewrote what we know about custom endpoints. 72 offline tests green.

**Landed (offline-verified, committed per task):**

- **T2 — bootstrap machinery.** `bootstrap.py` builds the package zip from
  in-repo `bootstrap_project.xml` and drives
  import → publishBegin → publishEnd-poll via new CustomizationApi methods
  on `AcumaticaClient` (same cookie session). `acu bootstrap` publishes to
  the session tenant; idempotent skip when `getPublished` lists it. The
  poll tolerates transport errors (publish restarts the app domain).
- **T1 — `acu provision`.** One command: tenant create (skipped if the
  login name exists) → recycle + login-check (factored `_init_tenant`) →
  bootstrap publish → apply `bootstrap/*.yaml` (new optional per-file
  `endpoint:` override targets the custom endpoint) → apply `baseline/` →
  diff (exit 1 on drift). Every session signs into the provisioned tenant.
- **T4 — tenant guard.** `AcumaticaClient` now refuses to open a session
  without an explicit tenant — the omitted/empty tenant is the one login
  the server still routes silently (to the default tenant). Mechanical V5.

**T3 — the verification that answered "no."** Question from the spec: does
CS100000 (Enable/Disable Features) accept writes through a custom endpoint?
Getting the answer took reverse-engineering how custom endpoints actually
work, because the first `acu bootstrap` against a scratch tenant failed
with a misleading "previously published project cannot be found" — which
turned out to be the CustomizationApi's way of saying the import had
already failed. Findings, all live-verified (see docs/rest-api.md):

- **The CustomizationApi reports failures in-band**: every call answers
  200; errors exist only as `log` entries with `logType: "error"`. Our
  `_checked` (HTTP-status-based) was blind to it — `AcumaticaClient` now
  has `_checked_log` and the import raises on in-band errors.
- **project.xml's root is `<Customization level description
  product-version>`**, not `<Project>` — verified against the training
  package Acumatica ships on the box itself
  (`HelpAndTraining/T270/PhoneRepairShop.zip`, same build). The project
  name comes from the import call. None of the shipped packages contains
  an endpoint item, so that serialization stays unverified —
  `PX.Api.ContractBased.Common.dll` carries `*.endpoint` file globs, which
  is the lead to chase.
- **A custom endpoint is just tenant-scoped rows in five tables**
  (`EntityEndpoint`, `EntityDescription`, `EntityFieldDescription`,
  `EntityMapping`, `EntityActionDescription`). Installed the Bootstrap
  endpoint as raw SQL rows on the scratch tenant and it went live in
  `GET /entity` instantly. The constants came from PowerShell reflection
  against the server's own DLLs: `ObjectType` T/L/D/R, field types are the
  wrapper names (`BooleanValue`, `StringValue`, …), mapping keys are
  `E/<entityId>/<fieldId>` → (view, DAC field), `SystemContractVersion`
  must be 4 on this build.
- **Landmines:** built-in endpoints materialize into the same tables under
  CompanyID 1 (EntityIds allocated globally — a colliding id kills the
  tenant's whole contract API with "An item with the same key has already
  been added"); endpoint metadata is cached per app domain (recycle to
  refresh); a malformed row 302s every `/entity` request on that tenant.
  Debugging visibility: flip `EnableFirstChanceExceptionsLogging` in
  web.config and read `App_Data\firstchanceexceptions.log` — the API's
  302-to-`/ui/error` carries nothing.
- **The verdict:** with the endpoint fully correct (swagger 200, field
  echoed in responses, tenant-scoping enforced), `PUT Features` returns
  200 and persists nothing — `FeaturesSet` stays empty. The primary view
  is a keyless BqlDelegate view; the graph save is a silent no-op. GET
  dies in `CannotOptimizeException` (BqlDelegate — CompaniesStructure's
  disease). Exposing and invoking the graph's `Insert` action:
  `PXInvalidOperationException`. **Features cannot be enabled through the
  contract API, full stop.** The spec's fallback — a C#
  `CustomizationPlugin` flipping `FeaturesSet` on publish — is now the
  route.

Cleanup verified: scratch tenant deleted, experiment rows removed,
web.config restored, `acu diff baseline/` green on Company.

Spec follow-ups queued for /sdd:spec: record the bug-class findings in §B,
new tasks for the C# plugin fallback and the `.endpoint` item format, §I
rows for `acu bootstrap` + the YAML `endpoint:` key, and CS101500/CS206500
write-path verification (normal keyed entities — likely fine, unlike
CS100000, but unverified).

## Evening — /sdd:build --all: the CLI-standard + config backlog lands (T5–T10); provision's live gate backprops into the spec

Second `--all` sweep of the day, six tasks. Five were mechanical
spec-conformance work; the sixth (layered config) produced both the day's
nicest deletion and a spec correction.

**Landed (each offline-verified, committed per task):**

- **T5 — ASCII sweep (V9).** `✓`/`✗` → `+`/`x`, `box.ROUNDED` → `box.ASCII`,
  spinner → `line` (the braille `dots` default violated ASCII-only on TTY),
  em-dashes out of every output-reaching string including click help text.
  New pin test renders table + all five helpers on a forced terminal and
  asserts `.isascii()` over both streams.
- **T6 — `docs/cli.md` deleted.** The output contract lives in SPEC §V now;
  CLAUDE.md and the two src docstrings that pointed at the file re-cite
  SPEC V9. Journal/design references stay — history, not live pointers.
- **T7 — drift is exit 2.** `_exit_on_drift` now raises `SystemExit(2)`
  (0 ok / 1 error / 2 drift); baseline repo's Makefile help text updated in
  that repo. Its working tree already carried unrelated edits, so the change
  waits uncommitted there rather than bundling someone else's diff.
- **T8/T9 — surface trims.** `acu bootstrap` command dropped
  (`bootstrap.publish()` module stays; resumable `provision` is the recovery
  route) and `schema -o` dropped (short flags are globals-only per V16).
  Both removals pinned by tests asserting the old spelling now fails.
- **T10 — layered `Instance` defaults + `acu config show`.** `host` is the
  only required `acu.toml` key; everything else is a code default
  transcribed from the verified docs, derived in a pydantic
  `mode="before"` validator (`base_url` = `http://<host>/AcumaticaERP`,
  `ssh` = `Administrator@<host>`), explicit overrides win.
  `acumatica-baseline/acu.toml` shrank from 8 spelled-out keys to
  `host` + `tenant` — every dropped value matched the new defaults exactly,
  which `acu config show` (same `load_instance` path, password masked)
  proved before anything live ran.

**Two findings worth keeping:**

- **The design doc's placeholders were wrong twice, and the spec's
  "transcribe from verified docs" clause caught both.** The draft said
  `base_url` derives as `https://<host>` at site root and `db_name` empty
  means "use `instance_name`" — but docs/rest-api.md records the verified
  base as `http://acu-dev1.vm.internal/AcumaticaERP` and the live DB is
  `AcumaticaDB` under instance `AcumaticaERP`. Defaults were transcribed
  from the docs, not the design; the design's own success criterion
  (minimal config passes live) is unreachable otherwise.
- **pydantic v2 silently ignores a non-`self` return from an `after`
  model-validator during `__init__`** (frozen-model derivation via
  `model_copy(update=…)` returns '' fields and a UserWarning you only see
  outside pytest's capture). The offline suite caught it as 20 failures.
  Derivation moved to a `mode="before"` validator reading defaults from
  `cls.model_fields` so literals aren't duplicated.

**And one backprop (B1 → V17 → T11).** T10's live gate said "minimal config
passes provision" — but provision's unconditional bootstrap-publish step is
the thing this morning's archaeology proved dead (CustomizationApi import
fails in-band; C# `CustomizationPlugin` is the route). The task's gate
depended on a capability the spec itself records as broken, with no queued
task to unblock it. Live run confirmed: config resolution, skip-create,
tenant list (SSH plane) and diff (REST plane, no drift) all green with the
two-line config; publish failed exactly as documented. Spec now carries
B1 (the bug class), V17 (a §T verify gate must be satisfiable against
current spec state, or cite the unblocking row), an amended T10 gate
(config-layer live checks — met), and T11 (the C# plugin fallback,
which also re-verifies provision end-to-end once it lands).

76 offline tests green; backlog is empty except T11.

## Late evening — T11: the CustomizationPlugin lands, and the import mystery dissolves

The C# features fallback (T11) is live-verified end to end — and getting
there overturned the morning's diagnosis of why the bootstrap package never
imported. It was never the endpoint item's fault.

**The import failure had three stacked causes, none of them the XML:**

- **`projectContents` binds nothing on 26.101.0225.** The live binder
  (`ImportParamsData`, found by reflecting `PX.Web.Customization.dll` over
  SSH) wants `projectContentBase64`. With the community-documented field
  name the server binds null content, `isReplaceIfExists` deletes the
  existing project, and the log reports "The project is not found" — the
  exact error we'd been attributing to endpoint serialization.
- **Project names are alphanumeric-only.** `CstDbStorage.ValidatePackageName`
  rejects `-` and `_` ("Invalid project name", import 500s). `acu-bootstrap`
  was an illegal name from birth; the package is now `AcuBootstrap`.
- The endpoint item *is* also broken (inline data-model `<Endpoint>` child →
  "Unknown root node"), but that only surfaced after the first two were
  fixed. It's out of the package until the `.endpoint` file format is
  discovered (SPEC T12).

**Code-item serialization, discovered by making the class testify.** No
shipped training package contains a source-code item, so the shape came
from reflection: `Customization.CstCodeFile` has `Tag = Graph`, and invoking
its own `Save()` on a scratch `XmlDocument` printed the canonical form —
`<Graph ClassName="X" FileType="NewFile" Source="…"/>` with the C# source
in the `Source` *attribute*. The plausible-looking alternatives (CDATA
child, zip file entry at the item's `FilePath`) import 200-clean and are
silently dropped — round-tripping through `getProject` is the only way to
see the loss. `package_zip()` now injects the item from a real
`bootstrap_plugin.cs` at build time; ElementTree's attribute escaping does
the rest.

**The plugin itself fought back twice:**

- `FeaturesMaint` + `Save.Press()` inside `UpdateDatabase` persists nothing:
  the publish pipeline invokes plugins around the site restart and the graph
  save collides ("Another process has added the 'FeaturesSet' record. Your
  changes will be lost." — logged as a *warning*, publish reports success).
  The working route is `PXDatabase.Update/Insert<FeaturesSet>` with all 205
  NOT NULL bit columns assigned reflectively (only ~136 have DB defaults).
  `Status = 0` = Validated (the PXIntList, read by reflection).
- The row landed and the screens stayed 403: the publish restarts the site
  *before* its DB transaction commits, so the fresh domain caches the
  pre-plugin feature set. One more app-pool recycle and
  `CustomerClass`/`ItemClass` went 200 (`VendorClass` moved to the
  setup-chain class — baseline seeding's job). `acu provision` now recycles
  unconditionally after its publish step.

**And one genuinely nasty resume trap (B3).** Deleting a tenant and
recreating it under the same CompanyID keeps the *publication* row —
`getPublished` still lists the package — while the project content and the
plugin's writes died with the tenant. The old "already published" skip left
the virgin tenant unbootstrapped, and an `isOnlyDbUpdates` replay fails
with "The previously published project cannot be found in the database"
(the morning's misleading message, now fully explained). The skip now
requires `getPublished` AND `getProject` to agree; V4 gained the general
rule: a resume/skip gate must verify desired state, never a marker.

Live verification: virgin-tenant `acu provision --id 3 --login Scratch`
exit 0 (create → publish → recycle → apply → diff), FeaturesSet row present
with the six flags, formerly-gated screens answering, stale-publication
resume re-publishing correctly. Cleanup: scratch tenant deleted, probe
projects deleted, web.config first-chance logging restored, Company tenant
diff green. 78 offline tests. Backlog: T12 (.endpoint discovery) is the
last blocker before company/credit-terms seeding.

## Night — T12: the `.endpoint` file that never was; the endpoint comes home

The last blocker before company/credit-terms seeding was "discover the
`.endpoint` package-file serialization" — the lead being `*.endpoint` /
`*.endpoint.remove` glob literals in `PX.Api.ContractBased.Common.dll`.
The discovery verdict inverted the premise: **there is no `.endpoint` file
in a package.** The globs belong to the source-control folder layout
(project-from-folder), not the zip. Endpoint items are plain inline XML —
the original import rejection was one wrong string all along.

**How it fell (reflection, no decompiler):**

- `Customization.CstEntityEndpoint` (PX.Web.Customization.dll) is an
  `IPersistObject` with `Save(XmlNode)`/`Export(XmlNode)` — so the item
  lives *inside* project.xml. Its `_representation` field is an
  `IEndpointSchema`; the implementation `XmlEndpointSchema` has a ctor
  taking `PX.Api.ContractBased.Common.Model.Endpoint` — a
  `GeneratedCode`-attributed XmlSerializer model. Serializing a sample
  instance in PowerShell printed the exact wire format: root
  `<Endpoint xmlns="http://www.acumatica.com/entity/maintenance/5.31"
  name version systemContractVersion>`, entities as `<TopLevelEntity
  name screen>` with `<Fields><Field name type/></Fields>` and
  `<Mappings><Mapping field><To object="view" field="DAC prop"/>`.
- The old package's child used
  `xmlns="http://www.acumatica.com/entity/data-model"` — hence "Unknown
  root node …data-model:Endpoint". Wrong namespace, not missing file.
- Live proof: imported a probe package with the maintenance/5.31 child —
  import 200 clean, and `getProject` round-tripped the endpoint verbatim
  (the server's own re-serialization even drops the `<EntityEndpoint>`
  `name` attribute; identity comes from the child). Probe project deleted.

**The payload names were also training-data fiction (B2 class, again).**
Read off the live box (aspx headers + DAC reflection): CS101500 is
`OrganizationMaint` with PrimaryView **`BAccount`** (DAC
`PX.Objects.CS.DAC.OrganizationBAccount`) — the old draft's `Organization`
view, `OrganizationCD` and `CountryID` fields do not exist on this build;
the real fields are `AcctCD`/`AcctName`/`OrganizationType`/`BaseCuryID`.
CS206500 is `TermsMaint` with PrimaryView **`TermsDef`** (DAC
`PX.Objects.CS.Terms`); `DayDue00` is `Nullable<Int16>` → `ShortValue`,
not `IntValue`. Contract field names now equal DAC property names verbatim
— no rename layer to drift. Features (CS100000) stays out of the endpoint:
T3's verdict stands, the plugin owns it.

**Dead end within the discovery:** instantiating `CstEntityEndpoint`
offline to watch its `Save()` output dies on "ServiceLocationProvider must
be set" — item classes resolve `IMaintenanceService` from the app's service
locator. The import round-trip through the real app was the cleaner
instrument anyway.

Live verification: provision E2E on a scratch tenant (id 3, T12Probe) exit
0 with the endpoint riding the package — `GET /entity` lists
`Bootstrap/1.0.0`, `GET Bootstrap/1.0.0/Company` and `…/CreditTerms` answer
200 on the company-less tenant, Default endpoint not poisoned (the probe's
first "poison check" used `Currency`, which 500s on any company-less tenant
by design — §C's documented state, replaced with seeded `UnitsOfMeasure`).
Scratch tenant deleted; standing `make diff` green. 79 offline tests, the
new one pinning the endpoint serialization. Bootstrap YAML seeding
(company + credit terms through `Bootstrap/1.0.0`) is now unblocked.

## Small hours — T13: company + credit terms seed from YAML; the screen's bindings are the mapping truth; the contract API speaks labels

The last bootstrap gap closed: `bootstrap/company.yaml` +
`bootstrap/credit-terms.yaml` (new, in the data repo) now seed CS101500 and
CS206500 through `Bootstrap/1.0.0`, and the **write path is live-verified**
— T12 had only proven GET. Getting the first `PUT Company` to stick took
three rounds of 422 archaeology, each one a keeper:

**Round 1 — the graph inserts rows you didn't send.** `PUT Company` with the
four endpoint fields died 422: *"Inserting 'Address' record raised at least
one error… CountryID: 'Country' cannot be empty."* OrganizationMaint inserts
the org's Main Address row on first save, and its CountryID is required.
The endpoint gained `CountryID -> AddressDummy` (the only Address-typed view
on the graph; reflected over SSH). Country rows live under CompanyID 1 only
(248 of them, the shared split) — so `US` validates on a virgin tenant.

**Round 2 — a DAC prop existing on the primary view is not enough.** Next
422: *"Inserting 'Company' record… BaseCuryID cannot be empty"* — while the
response happily **echoed back** `BaseCuryID: USD`. The BAccount projection
accepts the value and does nothing with it; the GL `Company` row the graph
inserts stays empty. The truth is the screen's own bindings: CS101500.aspx
binds OrganizationType and BaseCuryID to **OrganizationView**
(`GL.DAC.Organization`), not the primary view. Remapped both; PUT landed.
Rule worth keeping: **map endpoint fields to the view the screen edits —
an echoed-back value proves nothing** (the CS100000 lesson, one layer down).

**Round 3 — list fields speak external labels, not DAC codes.**
`VisibleTo: A` was rejected with the allowed list spelled out ("All,
Vendors, Customers, Disabled") — but `DueType: D` was **silently misread**
as "Day of Next Month" (a wrong-but-valid code is worse than a rejection;
it then demanded a DayDisc 1-31). Sending a deliberately bogus value is the
cheap way to elicit the full allowed-label list from the 422. The YAML now
carries `Fixed Number of Days` / `All` / `Without Branches` — labels, with
the allowed lists recorded in the file comments.

**Two code fixes fell out:**

- `seed._norm` compared numbers by spelling: YAML `0` vs the endpoint's
  `0.0` (DecimalValue fields come back as floats) flagged spurious drift.
  Numbers now compare by value; pinned offline.
- `provision` diffed only `baseline/` — the bootstrap YAML it had just
  applied was excluded from the drift check, exactly the class of trust
  T3 taught us to distrust (a PUT that answers 200 can persist nothing).
  The diff now covers everything applied.

Live verification: virgin-tenant `acu provision --id 3 --login T13Probe`
exit 0 — create → publish → recycle → apply (company + credit terms +
UOMs) → **no drift, 3 files**. Resume path re-verified on the existing
tenant. Cleanup: probe tenant deleted, standing Company `make diff` green.
80 offline tests.

**Noted for the spec:** the publish skip gate verifies the project *exists*
(`getPublished` + `getProject`), not that its content matches what this
tool version would publish — during T13's iteration a changed package
would have silently skipped on the already-provisioned tenant (dodged by
recreating the tenant). Same V4 marker-vs-state class as B3, one notch
subtler. Also: the utf-16le/base64/`-EncodedCommand` reflection probe was
hand-rolled twice more tonight (graph views, aspx bindings) — plus a
quoting trap (the remote shell is PowerShell: `$_` inside a double-quoted
`-Command` string is interpolated away before the inner powershell sees
it). T14's case grows.

## Config file: TOML → YAML, and `config show` becomes the config editor

The operating friction: `acu config show` printed flat `key = value` lines
of the resolved `Instance` while the source file was nested TOML
(`[instances.<name>]`) — the output could not be pasted back into the
config. The desired workflow is `acu config show > acu.yaml`, then edit.

Weighed three routes: keep TOML and grow a TOML emitter (stdlib `tomllib`
is read-only; a writer means hand-rolling or a new dependency), flat ENV
(dead on arrival — loses the multi-instance nesting and collides with V2's
env-is-secrets-only split), or YAML. YAML won on every axis: PyYAML is
already a dependency (`baseline/*.yaml`), `yaml.safe_dump` gives a
round-trippable emitter for free, and the ecosystem converges on one
format.

Landed as T15: `acu.yaml` is the discovery sentinel, the loader is
`yaml.safe_load` (empty or non-map file is a hard error — `safe_load`
returns `None` for an empty file, an easy crash class), and `config show`
emits a complete valid `acu.yaml`: header comment, `default_instance`,
the fully resolved instance map. Credentials and the injected `name`
field are excluded, which is exactly what makes the output legal input —
the `Instance` model is `extra="forbid"`, so any stray field would refuse
to load. The round trip is pinned by an offline test and verified live:
`config show > acu.yaml` in a scratch dir, re-show from there,
byte-identical output; `tenant list` green through the new loader.

One scope correction mid-flight: the first V3 draft had the discovery
error hinting at a leftover `acu.toml` ("rename me"). Dropped by operator
call — no migration logic in spec or code; the old file gets cleaned up
manually. The data repo carries the converted `acu.yaml` (two keys plus
comments); its `acu.toml` awaits manual deletion.

## Config flattened: one host, one file, no instance selector

Asked whether `default_instance` earns its place, the multi-host question
got its real answer: the plan was dev1/tst1/prd1, but multiple instances
means multiple admin passwords in one `.env` — a problem not worth
carrying for a capability nobody uses yet. Operator call: single-instance
config, no `default_instance`, one Windows host for now.

Landed as T16, mostly by deletion. The `instances.<name>` nesting,
`default_instance`, and the `-i/--instance` global are gone; `acu.yaml`
is now a flat map that *is* the instance (`host` + optional overrides,
two lines in the data repo). The `Instance.name` field went with them —
it was only ever the map key — so messages now label the target by
`host` (`no drift on acu-dev1.vm.internal/Company`). `config show`
emits the resolved map directly and the round trip
(`acu config show > acu.yaml`) got simpler and stayed byte-identical,
re-verified live along with `tenant list`.

No migration logic, again by operator call: a legacy nested file dies on
pydantic's `extra="forbid"` naming `default_instance`/`instances` as the
offending keys — pinned as a test, which is as much migration signal as
a one-user tool needs. If multi-host ever really arrives, re-nesting is
an accepted format migration (and the per-instance password question
comes back with it).

## T14: the reflection probe becomes a script

The utf-16le/base64/`-EncodedCommand` pipeline that T3, T11, and T12 each
hand-rolled (and T13 hand-rolled twice more) is now `scripts/ps-remote
<file.ps1> [user@host]`. The whole point is the encoding: the remote ssh
shell is PowerShell, so any `-Command` string crosses *two* PowerShell
parsers and a POSIX shell — `$_` interpolates away, quotes need triple
escaping. `-EncodedCommand` carries the script byte-exact through all of
it.

Host resolution reuses `acu config show` (sed out the `ssh:` line) rather
than re-parsing `acu.yaml` in shell — the same no-parallel-resolution rule
that shaped `config show` itself; an explicit `user@host` second arg skips
it entirely. `exit $LASTEXITCODE` appended per the §I.ssh idiom. Pinned
offline by stubbing `ssh` and `acu` on PATH: the encoded payload decodes
back byte-exact, exit codes propagate, explicit host never consults `acu`.
